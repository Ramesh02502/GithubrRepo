pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "jenkin Step"
            }
        }
        stage('Test') {
            steps {
                echo "Test Step"
            }
        }
        stage('Deploy') {
            steps {
                echo "Deploy Step"
            }
        }
    }
}
